import MovieContainer from './MovieContainer';
import SearchForm from './SearchForm';

const Landing = (props) => {
    return(
        <div>
            <h1>Landing</h1>
            <SearchForm/>
            <MovieContainer/>
        </div>
    )
}
export default Landing 